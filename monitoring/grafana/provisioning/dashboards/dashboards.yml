# Grafana가 지정 폴더의 대시보드 JSON을 자동으로 불러오게 한다.
apiVersion: 1

providers:
  - name: "study-calendar"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
